import { AnalyticsPage } from "@/features/workspace/analytics/pages/AnalyticsPage";

/** Reuses workspace analytics layout for now; data remains scoped to signed-in user. */
export function ClientAnalyticsPage() {
  return <AnalyticsPage />;
}
