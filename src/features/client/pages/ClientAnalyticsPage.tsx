import { AnalyticsPage } from "@/features/admin/analytics/pages/AnalyticsPage";

/** Reuses admin analytics layout for now; data remains scoped to signed-in user. */
export function ClientAnalyticsPage() {
  return <AnalyticsPage />;
}
